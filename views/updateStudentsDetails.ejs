<!-- Include the navigation bar component from "navbar.ejs" -->
<%- include("navbar.ejs")%>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Student Details</title>
    <!-- Include CSS stylesheets for styling -->
    <link rel="stylesheet" href="/style/updateStudentsDetails.css">
    <link rel="stylesheet" href="/style/navbar.css">
</head>
<body>
    <div class="container">
        <h2>Update Student Details</h2>

        <!-- Create a form for updating student details with a POST request to "/updateStudent/<%- id %>" endpoint, where "<%- id %>" will be replaced with the actual student's ID -->
        <form action="/updateStudent/<%-id %>" method="post">
            <div class="row">
                <div class="row-item">
                    <label for="fullname">Full Name</label>
                    <input type="text" id="fullname" name="fullname" value="<%-allSingleDetails[0].fullname %>" placeholder="your vaild fullname" required>
                </div>

                <div class="row-item">
                    <label for="address">Address</label>
                    <input type="text" id="address" name="address"  value="<%-allSingleDetails[0].address %>" placeholder="your vaild address" required>
                </div>
            </div>
    
            <div class="row">
                <div class="row-item">
                    <label for="grade">Grade</label>
                    <input type="number" id="grade" name="grade" value="<%-allSingleDetails[0].grade %>" placeholder="your grade" required>
                </div>

                <div class="row-item">
                    <label for="rollno">Roll number</label>
                    <input type="number" id="rollno" name="rollno" value="<%-allSingleDetails[0].rollno %>" placeholder="your roll number" required>
                </div>
            </div>
    
            <div class="row">
                <div class="row-item">
                    <label for="age">Age</label>
                    <input type="number"  id="age" name="age" value="<%-allSingleDetails[0].age %>" placeholder="your age" required>
                </div>

                <div class="row-item">
                    <label for="contactno">Contact Number</label>
                    <input type="text" id="contactno" name="contactno" value="<%-allSingleDetails[0].contactno %>" placeholder="Your contact number" required>
                </div>
            </div>
    
            <div class="row">
                <div class="row-item">
                    <button class="add-button">Update</button>
                </div>
            </div>
        </form>
    </div>
    


</body>
</html>
